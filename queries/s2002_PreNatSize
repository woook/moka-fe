SELECT Mid([Chr],4,2) AS C, Mid([Chr],4,2) & [band] AS CytoBand, TempPreNat.Start, TempPreNat.Stop, TempPreNat.Probe, TempPreNat.RatioPos, TempPreNat.RatioNeg
FROM TempPreNat
WHERE ((([Stop]-[Start]+1)>399999));
